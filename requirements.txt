# JScaner Requirements - Updated December 3, 2025
# Current Setup: Python 3.11 with RTX 3060 GPU Acceleration

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
Pillow>=10.0.0
tqdm>=4.65.0
scikit-image>=0.21.0

# Kinect v2 support (Optional - for Xbox 360 Kinect)
# Note: pykinect2 has compatibility issues on Python 3.11+ (struct size mismatch)
# Currently using libfreenect2 (vcpkg) + test pattern display as workaround
# pykinect2>=0.1.0  # Requires Microsoft Kinect SDK installation (Windows only)

# GPU acceleration (NVIDIA RTX 3060 with CUDA 12.4)
numba>=0.62.0     # JIT compilation with CUDA support
numexpr>=2.8.0    # Fast vectorized operations
bottleneck>=1.3.0 # Optimized NumPy functions

# 3D processing (Full support with Python 3.10+)
open3d>=0.17.0  # Primary 3D reconstruction engine
trimesh>=4.0.0  # Mesh processing and STL export

# GUI (built-in with Python)
# tkinter - included with Python standard library

# Development and testing (optional)
# pytest>=7.0.0
# black>=23.0.0
# mypy>=1.5.0

# Installation commands:
# 
# Current Setup (Python 3.10 + RTX 3060):
#   pip install opencv-python numpy scipy scikit-learn matplotlib Pillow tqdm cupy open3d trimesh scikit-image
#
# GPU Acceleration Only (requires CUDA 13.0 installed):
#   pip install cupy
#
# Verify Installation:
#   python -c "import cupy; print(f'CuPy {cupy.__version__}')"
#   python main.py